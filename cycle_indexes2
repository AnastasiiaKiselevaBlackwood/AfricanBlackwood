first_string = 'В еиоен'
second_string = 'ывлклпы'

final_string = '' 
for index in range(len(first_string)):
    final_string = final_string + first_string[index] # ваш код здесь
    final_string = final_string + second_string[index]
    
print(final_string) # выводим результат на экран
